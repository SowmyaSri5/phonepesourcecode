import 'package:flutter/material.dart';
class Phpeims extends StatelessWidget {
  const Phpeims({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
      Container(
      width: double.infinity,
      color: Color.fromARGB(100, 158, 139, 211),
        child:

        Image.asset("assets/images/in.jfif",),
      ),

      ],
    );
  }
}
