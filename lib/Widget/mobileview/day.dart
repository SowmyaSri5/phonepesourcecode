import 'package:flutter/material.dart';
class Colpic extends StatelessWidget {
  const Colpic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset("assets/images/insu.jpeg"),

      ],
    );
  }
}
